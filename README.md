Program count words from original string
Program make reverse string from original string
Program use one string, that is inside the code. Given own string is not possible

compile program:

run javac count.java

run program:

java count


==========
